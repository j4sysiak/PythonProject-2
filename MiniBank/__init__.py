"""MiniBank package marker.

Makes the `MiniBank` directory a Python package so that package-relative imports
(like `from .database import ...`) work when importing `MiniBank.main` from tests.
"""

__version__ = "0.0.0"

